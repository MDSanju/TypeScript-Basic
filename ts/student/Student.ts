const studentName: string = "Roja";
const studentClass: number = 8;

export const studentInfoFunc = (studentAge: number): void => {
  console.log(
    `Name: ${studentName}, Class: ${studentClass}, Age: ${studentAge}!`
  );
};
